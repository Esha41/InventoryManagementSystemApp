/** Max quantity for which "fill identifiers" remains enabled in bulk mode. */
export const WEAPON_ASSET_MAX_FILL_IDENTIFIERS_QUANTITY = 100;

/** Max bulk template quantity (must match backend `CreateBulkAssetsFromTemplateDtoValidator`). */
export const WEAPON_ASSET_MAX_BULK_QUANTITY = 1_000_000;
